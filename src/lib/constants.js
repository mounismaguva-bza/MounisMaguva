/** Maximum products allowed in the catalog (Firestore + storefront). */
export const MAX_PRODUCTS = 30;

/** Maximum photos per color on a single product. */
export const MAX_IMAGES_PER_COLOR = 4;

/** Maximum slides in the homepage hero carousel. */
export const MAX_HERO_SLIDES = 6;
